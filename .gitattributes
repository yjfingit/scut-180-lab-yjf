# 统一换行符为 LF —— 保证 PC(Windows) / GitHub / 180机(Linux) 三处字节一致
# 不加这个的话，Windows 的 core.autocrlf=true 会把 LF 转成 CRLF，
# 导致 PC 上的文件 hash 与 Linux/GitHub 版本不同，同步校验永远失败。

* text=auto eol=lf

# 明确声明：shell 脚本必须是 LF，否则 Linux 上执行会报 bad interpreter
*.sh        text eol=lf
*.py        text eol=lf
*.md        text eol=lf
*.txt       text eol=lf

# 二进制文件不做换行转换
*.png       binary
*.jpg       binary
*.gz        binary
*.zip       binary
